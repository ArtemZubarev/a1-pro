export default function Algorithm() {
  return (
    <div className="font-mono text-red-500">
      {/* const moveZeros = (arr) => {
        let zeros = [];
        
        for (let i = 0; i < arr.length; i++) {
          if (arr[i] === 0) {
            zeros.push(arr[i]);
            arr.splice(i, 1);
            i--;
          }
        }
        return [...arr, ...zeros];
      } */}

      <iframe width="100%" height="300" src="//jsfiddle.net/tommySaggio/f6umLgwx/3/embedded/js/dark/"></iframe>
    </div>
  )
}